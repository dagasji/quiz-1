<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/stylesheets/style.css" />
        <title>Quiz</title>
    </head>
    <body>
        <h4> Resultado: </h4>
			<p><h2><%= respuesta %></h2></p>

			<% if( respuesta === 'Correcto'){%>
				<a href="/quizes">Volver al listado de Preguntas</a>
			<%}else{%>
				<a href="/quizes/<%= quiz.id %>"> Reintentar</a>
			<%}%>

    </body>
</html>
